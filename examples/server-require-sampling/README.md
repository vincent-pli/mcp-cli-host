# A MCP server with sampling request

## Setup & Running

1. Navigate to the samples directory:

   ```bash
   cd examples/server-require-sampling
   ```

2. Run the agent:

   ```bash
   uv run server-require-sampling